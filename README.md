Repositório de estudo de docker

Instalação de PHP e Lavarel no container.

# Como rodar

```bash
    docker build -t kafernandesdev/laravel:latest .
    docker run --rm -d --name laravel -p 8009:8000 kafernandesdev/laravel
```
